# Filename: /mnt/d/__PROJECTS__/LARAVEL/advanced-todo-app/docker/php/000-default.conf

<VirtualHost *:80>
    # 1. CHANGE THIS BACK to the root
    DocumentRoot /var/www/html

    <Directory /var/www/html>
        # 2. CHANGE THIS BACK to match
        AllowOverride All
        Require all granted
    </Directory>

    # Standard logging
    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>